import { createContext } from "react";

export const AccountsContext = createContext([]);

export const userApiUrl = "http://127.0.0.1:5001/accounts";
// export const userApiUrl = "https://jsonplaceholder.typicode.com/users";